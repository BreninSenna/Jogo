// Criar variaveis no Javascript
//var letra = "o"; var é uma versão em desuso
//const letra = 'd';
//console.log(letra);

//Iniciando uma variável que representa um jogador
let letra = 'X';
function joga(cel) {
    let casaClicada = document.getElementById(cel).innerHTML;
    //innerHTML injeta tags HTML na página
    if(casaClicada == 'X') || casaClicada="O") {
        
    }
}